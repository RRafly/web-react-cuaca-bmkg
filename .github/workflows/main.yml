on:
  push:
    branches:
    - master
    
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4
    # - name: Use Node.js ${{ matrix.node_version }}
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: ${{ matrix.node_version }}
    
    - name: Install
      run: npm install

    - name: build
      run: npm run stage-gh
  
