on:
  push:
    branches:
    - master
    
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     node-version: [18.x, 20.x, 22.x]

    steps:
    - name: Install
      run: npm install

    - name: build
      run: tsc -b && vite build --mode development
  
